/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://niagaradataanalyst.com',
    generateRobotsTxt: true, // (optional)
     // Default transformation function
  }